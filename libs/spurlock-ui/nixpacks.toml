[phases.setup]
nixPkgs = ['nodejs_20', 'pnpm']

[phases.install]
cmds = ['cd ../.. && pnpm install --frozen-lockfile']

[phases.build]
cmds = ['cd ../.. && pnpm build:storybook']

[start]
cmd = 'cd ../.. && npx serve dist/storybook/spurlock-ui -s -p $PORT'
