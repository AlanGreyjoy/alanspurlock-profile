// This file is kept for compatibility but routing is now handled in router.tsx
// The app entry point is now main.tsx which uses RouterProvider

export function App() {
  return null;
}

export default App;
