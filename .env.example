# Frontend URLs
VITE_STORYBOOK_URL=http://localhost:6006
VITE_API_URL=http://localhost:3000/api

# Database Configuration
# For local development (using docker-compose on port 5433):
DATABASE_URL="postgresql://spurlock:dev_password@localhost:5433/resume_db"

# For production (Railway):
# DATABASE_URL="postgresql://user:password@host:port/db"

# Logging Configuration
# Log level: debug, info, warn, error, fatal (default: debug in dev, info in prod)
LOG_LEVEL=debug

# Node environment (affects log formatting)
NODE_ENV=development
